services:
  qr-generator:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PROJECT_NAME=Qr Generator API (Docker)
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
